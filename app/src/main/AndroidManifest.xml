<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mangareader">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MangaReader">
        <activity android:name=".HistoryPage.HistoryActivity"
            android:windowSoftInputMode="adjustPan"
            >

        </activity>
        <activity android:name=".MorePage.NotificationsPage.NotificationsActivity" />
        <activity android:name=".MorePage.MoreActivity" />
        <activity android:name=".ReaderController.ReadingActivity" />
        <activity android:name=".MainMenu.MainActivity"
            android:windowSoftInputMode="adjustPan"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>